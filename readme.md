# Fashion Amigo iOS

This is the Capacitor wrapper for Fashion Amigo PWA to build native iOS apps.

## Configuration

Update the `server.url` in `capacitor.config.json` with your Fashion Amigo web app URL.
